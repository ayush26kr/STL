//Running stream means we can not store all the numbers since they are not definite and keep on comming 
//we have to find k largest elements 
//we will be using min heap for it
#include<iostream>
#include<cstdio>
using namespace std;
int main()
{
    int no;
    while(scanf(%d,&no)!=EOF)
    {
        cout<<no<<",";
    }
}